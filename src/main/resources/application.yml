spring:
  profiles:
    active: dev
  datasource:
    url: "jdbc:postgresql://${database.host}:${database.port}/${database.name}"
    username: "${database.username}"
    password: "${database.password}"
    driver-class-name: "org.postgresql.Driver"
    flyway:
      enabled: true

database:
  host: "localhost"
  port: 5432
  username: "root"
  password: "password"
  name: "security"

server:
  port: 8080

jwt:
  secret: "8be5946d3b667b31b286ddd447c5cd948f7a6692e8603218d18e718344da323426d80aee0982b4a7816953d5787ff2a868e46833a3575454b548c205cf9aa789"